## flaggen

Usage: `python3 flaggen.py`

This will load the "data" file in text mode.

If you need a custom file,

Usage: `python3 flaggen.py [file]`

And if the file has hex numbers written as strings, do

Usage: `python3 flaggen.py [file] hex`